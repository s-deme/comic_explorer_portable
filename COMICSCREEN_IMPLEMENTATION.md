# ComicScreen 2300への設定・UI対応

## 2026-09-20 ユーザー指定による読書UIの変更

現在の読書画面はページ一覧を中央表示に一本化し、ドロワー・帯状サムネイル・全画面切替を廃止。上部は一覧・送り方向・レイアウト・フィルター・余白トリミングの5ボタン、しおり・明るさ・自動送りは読書メニューから操作する。以下の履歴にある旧表示方式より、この指定を優先する。

テストの現行構成は [2026-09-18 テストの最小化](DEVELOPMENT.md#2026-09-18-テストの最小化) を参照。以下の検証件数は各実装時点の記録です。

更新日: 2026-09-18。広告・課金以外を参考アプリへ寄せる依頼に対する作業ツリーの実装記録。
変更前の比較は [COMICSCREEN_COMPARISON.md](COMICSCREEN_COMPARISON.md)。

## 2026-09-18 データ移行を除く追加対応（最新）

- 初期値をグリッド4列・背景 `#303030`・縦スクロール境界線OFFへ統一。保存済み設定は維持する。
- 単色画像にも参考側の輝度正規化を適用。反転はRGB反転と透明度チャネル1、API 31/32では背景色の加算補正を実装。不透明画像はRGB565で出力し、不透明な表示を維持する。
- 画像上のタップは位置にかかわらずメニュー表示へ変更。スワイプとページボタンで移動する。4言語の操作説明も更新。
- 連続スクロールは初期値23spの重なりを残す移動量へ変更。距離から所要時間を計算し、5次の減速補間を使う。従来の保存済み割合設定は、新しい重なり設定を操作するまで保持する。
- RAR4/5、暗号化アーカイブ、分割RARと `.7z.001` / `.zip.001` の読み込みを追加。ZIPの通常読み込みでは指定文字コードを維持。分割ファイルのURIを追加選択できる。アーカイブのエントリー名を出力パスとして使用しない。
- 作品パスワードの入力・再入力・キャンセルを追加。パスワードは読書中のメモリのみで保持する。ファイル単体の7zは従来の64 MiBメモリ検査を維持し、ネイティブ抽出でも辞書サイズを確認。展開画像は48 MiBまで。
- PDFの目次を読み、章一覧から対象ページへ移動できる。パスワード付きPDFにも対応。復号した一時ファイルはPdfRendererが開いた後に削除し、開いたファイル記述子だけを使用する。
- GIFを時間に応じて再生。各フレームに同じ切り抜き・画像処理・見開き合成を適用し、単ページと連続表示に対応。画面を離れた場合やビューの再利用時には停止する。描画更新は最大約30fpsで、処理時間に応じてフレームを間引く。
- データ移行は今回の変更対象外。Google Drive・SMB・FTPSは既存実装を確認したが、実接続先・OAuth環境が提供されていないため実サービスの検証は未完了。

根拠は指定APKの静的解析。参考アプリの実際の閲覧画面が起動できないため、アイコン・余白・速度の完全一致は保証しない。追加依存は7-Zip-JBinding-4AndroidとPDFBox-Android。7-Zipライブラリの対応ソースを配布APKと同じ `dist/` に同梱する。

検証結果は Instrumentation **142項目成功**、形式判定・自然順ソート **26項目成功**、Lint **0 errors / 46 warnings**。記録は `build/parity-next-build.txt`、`build/parity-next-test.txt`。自作RAR5・分割RAR5・分割7z・暗号化7z、PDF目次・暗号化PDF、GIFのフレーム変化と実画面再生、反転の透明度、明色グリッドの文字コントラストを確認。パスワード入力・明色グリッド・横画面切り抜きの画像を `build/parity-next-screenshots/` に保存して目視確認した。

最終APKは `dist/comic-explorer.apk`、176,014,815 bytes（約167.9 MiB）。既存署名・権限・16 KiB zipalign・arm64-v8a/x86_64の全ネイティブELF整列を検証済み。AndroidXの署名保護されたアプリ内部用権限を配布検査の許可リストへ追加した。SHA-256: `9E819541AE0FB7CE3DDDEA8CEF1667F1DE67DCE733272358E67943D3CE6ACAA0`。LGPLライブラリの対応ソースは `dist/sevenzip-source-16.02-2.03.zip`（SHA-256: `55F0851C30FEC3EDCB1B374412CA1B346E07FC8C4A4BD35C8E9A4DEC3C8F5E2E`）。

以下は以前の変更履歴で、最新の挙動は本節を優先する。

## 2026-09-18 残差への追加対応（前回）

- ページ・章一覧をAndroidX DrawerLayoutへ変更。左端ドラッグ、外側タップ、戻るで開閉し、現在位置へ移動する。ページ一覧は白背景、章一覧は文字のみ。従来の切替タブ・閉じるボタンを除去。
- 切り抜きを独立画面にし、4隅・4辺のドラッグ、範囲内の移動、4辺スライダー、初期化、キャンセル、適用を用意。閲覧画面の向きを継承し、画面再生成でも選択範囲を復元する。
- 画像の慣性移動にOverScrollerを使用。ダブルタップは現在倍率への乗算と画像内／外の判定、フィット倍率の切替、150 msの加減速アニメーションを実装。標準の上限は5倍、極端な縦横比では上限を拡張。初期値はOFF・180%。
- OpenCV 4.12.0のRGBAグレー変換、YUV輝度の自動コントラスト、Linear／Bicubic／Lanczos4補間、5×5 GaussianBlurを用いたシャープ処理へ変更。順序はグレー→コントラスト→拡大→シャープ→反転。ブルーライトは反転状態と強度から算出した色乗算を適用する。
- フィルターの無効項目に属する補助操作を隠し、背景を暗くせず結果を確認できるようにした。補間は初期値Lanczos、シャープ強度0、ブルーライト50。既存の保存値は維持。
- 設定のINFORMATIONに参考データの取り込みを追加。選択した `basedata` SharedPreferences XMLの既知11設定と、`DB_DATA.db` の履歴・しおり・お気に入りを検査し、件数確認後に反映。既存の読書位置・しおりメモを維持する。設定は対応項目のみ上書き。履歴表示は既存仕様の100件。

今回の根拠は指定XAPKのリソースに加えて、画像ビュー・ジェスチャー・画像処理・設定保存・DBスキーマの静的解析。解析用ファイルは `build/comicscreen-audit/` 内に隔離し、参考アプリのコード・画像・ネイティブバイナリを製品へコピーしていない。OpenCVとDrawerLayoutは公式配布ライブラリを利用する。

取り込みはユーザーが別途用意したコピーのみを扱い、他アプリの非公開領域へアクセスしない。32 MiB／各表5,000行、XMLは2 MiBまで。型・値域・URI・XML宣言を検証する。元アプリ独自のアルバム形式、ネットワーク資格情報、未知の設定、アカウントは移行しない。保存先へのアクセス許可はAndroidの選択画面から再取得が必要。実データでの互換性は未検証。

画素処理の差異も残る。単色ページのコントラストは単色のまま維持し、反転では透明度を維持する。参考側の透明度設定やAndroid 12固有の背景補正までは再現していない。処理画像のサイズ・メモリ上限は本アプリの制限を維持する。慣性の係数、全タップ領域、連続スクロールの時間、画素単位の外観は参考アプリの動作比較ができていない。

検証: Instrumentation **122項目成功**、Lint **0 errors / 41 warnings**。記録: `build/full-parity-build.txt`、`build/full-parity-test.txt`、`build/full-parity-screenshots/`。OpenCVネイティブ処理、取り込みの反復時データ保持、左端スワイプ、切り抜きの向き・再生成・適用をAPI 37／16 KiBエミュレーターで確認。通常文字と1.5倍文字、横画面を撮影し確認。配布APKは `dist/comic-explorer.apk`。

最終APK: 159,689,261 bytes（約152.3 MiB）。公式OpenCVの4 ABIを含むため従来より大きい。既存と同じ署名証明書、権限検査、16 KiB zipalign、arm64-v8a／x86_64の全ELF LOAD整列を検証済み。SHA-256: `EF39390777AA156F45D9739FFA7817DB99EAF9C8F762EE6CD3BF86B8592621B3`。

以下の同日108項目の記録は今回の変更前の履歴。倍率上限・画像処理・サイド一覧・切り抜きの説明は本節を最新とする。

## 2026-09-18 画面・操作感の追加対応（前回）

- 元APKの `fragment_slide_index` と `fragment_slide_chap` に合わせ、ページ一覧を1列サムネイル、章一覧を文字のリストに変更。幅は元リソースの96/128 dp（ページ）・280/450 dp（章）。サムネイル帯の画像高は47/61 dp。端末幅と文字の可読性を優先し、閉じる操作とタブは残す。
- ダブルタップの4動作と補間方式を、元APKのダイアログ定義にあるラジオボタンで直接選択。既存設定の値は維持。
- 拡大・ドラッグ後に画像端を画面端で制限。幅／高さ合わせで画像がはみ出す場合も移動可能にし、その方向のドラッグをページ送りと競合させない。
- ピンチ中の親リストによるタッチ奪取を抑制し、指を離したときの追跡対象を引き継ぐ。ピンチとダブルタップの上限を6倍に統一。スワイプ閾値は端末のタッチ許容幅を基準にする。
- 余白カット・画像処理を各ページへ適用してから見開きを合成。中央線や合成時の余白を自動コントラスト・シャープ・色反転の対象から除外。拡大処理の中間画像も処理後に解放する。

検証: Instrumentation **108項目成功**、Lint **0 errors / 35 warnings**。通常文字と1.5倍文字のダブルタップ／フィルター設定を目視確認。更新APKは既存と同じ署名、署名・権限・zipalign検証済み。SHA-256: `335920EF1201C21F1A38E444C8DFA90FFD3E57A5D5CEA223CA2711678DF6C6E8`。

参考の根拠は抽出リソース。ドラッグ境界・画像処理順序は現行実装の問題を修正したもので、参考アプリの内部アルゴリズムや速度を再現したとの主張ではない。画像処理は既存のLinear/Bicubic/Lanczos、自動コントラスト等を維持する。

## 2026-09-17の追加対応

- ページ・章を画面左側の一覧へ変更。両一覧の切替、現在ページ付近へのスクロール、横スクロールのサムネイル帯を追加。
- 背景色に12色のパレットを追加し、任意の16進入力も維持。
- アルバムの作成・名前変更・削除、複数画像追加・登録解除。元画像を参照する方式で、アルバム削除では画像を消去しない。
- 複数選択、コピー／移動、一括連番変更、削除。別DocumentsProvider間の転送にも対応。未対応形式を含めフォルダ内の全ファイルを対象にする。
- 同名項目は両方保存／検証後の置換／スキップを選択。転送失敗時は元ファイルを維持し、作成途中のコピー先を削除。置換は別名へ転送してから切り替える。
- 移動前にコピー先のSHA-256を照合し、削除直前にも構成・内容を再検証。ネットワークでは閲覧キャッシュを経由せず読み直す。フォルダ移動・名前変更で配下の読書情報も引き継ぐ。
- RAR4／CBRと7z／CB7を追加。展開名をOSの保存パスとして使わず、一時ファイルへ展開。画像1枚48 MiB、20,000ページ、7zのヘッダー・LZMA系展開メモリ64 MiBを上限とする。
- 切り抜き範囲を作品別に保存。再オープン・名前変更・移動時も引き継ぐ。見開きでは元ページごとに適用する。
- 韓国語・ロシア語の未翻訳294項目ずつを補完。新規UIも日本語・英語・韓国語・ロシア語を収録。
- `file://`で渡された作品のキャッシュ更新判定に、サイズと更新時刻を追加。

追加依存はJunrar 7.5.5、Apache Commons Compress 1.28.0、XZ for Java 1.10。参考アプリのコード・画像資産は取り込んでいない。

検証は独立した `.validation` APKをAndroid 17 / API 37 / 16 KiBページサイズのPixel 7aエミュレーターで実行。追加のテスト用DocumentsProviderはandroidTest専用で、配布APKには含まれない。実行結果と画面は `build/parity-current-test.txt` と `build/parity-current-screenshots/` に保存。

検証結果: Instrumentation **97項目成功**、形式判定・自然順ソート **24項目成功**、Lint **0 errors / 33 warnings**。RAR4の自作無圧縮画像フィクスチャと7z/LZMA2でデコード・逆順移動を確認。転送は別authorityのテスト保存先で、フォルダ全内容・置換・しおり移行・書き込み失敗時の元データ保持を確認。FTPはループバックで送受信を確認。ページ・章一覧、サムネイル帯、色パレット、一括選択、アルバム、4言語の設定、横画面切り抜きを撮影し、主要な文字・操作の欠落がないことを目視確認。

## 対応範囲

| 区分 | 追加・変更した内容 |
| --- | --- |
| 設定画面 | 元APKの5分類・24項目を基準に配置。説明付きのチェック項目、選択ダイアログ、キャッシュ消去の確認 |
| 一般設定 | システム／明色／暗色、日本語・英語ほかの言語選択、設定のみ初期化、アーカイブ文字コード7種類 |
| 一覧設定 | ステータスバー、クイックビュー、パス、左スクロールバー、通常／次の作品で別々の再開方法 |
| 画像設定 | 全画面、見開き・縦スクロールの境界線、縦横個別のカットアウト利用 |
| キャッシュ | サムネイル容量、圧縮ファイルの保持日数、種類別削除、復帰位置・しおり・履歴の個別削除 |
| 一覧表示 | アイコン／サムネイル／グリッド、1〜10列、名前表示、正方形、背景色、タブ間スワイプ |
| 閲覧 | 縦連続スクロール、ページ間隔、次の作品、ページサムネイル、アーカイブ内フォルダ単位の章ジャンプ |
| 操作 | ページボタン4種類・4位置、反転、両方次へ、固定、サイズ・不透明度、スクロール量・アニメーション、キー割り当て |
| 拡大・画像処理 | ダブルタップ4動作・倍率、グレースケール、自動コントラスト、拡大補間3方式、シャープ、反転、ブルーライト強度を併用 |
| 切り抜き | ドラッグと4辺のスライダー。原本は変更せず閲覧画像へ適用 |
| ファイル操作 | 保存先が対応するフォルダ作成・名前変更・移動・削除。単一作品の位置・しおり・メモ・お気に入りを名前変更／移動後へ引き継ぐ |
| ギャラリー | Androidメディアストアと選択フォルダ内の画像検索 |
| ネットワーク | SMB／FTP／FTPS接続先登録、DocumentsProvider経由で一覧・閲覧・対応するファイル操作。パスワードはAndroid Keystoreで暗号化 |
| 同期 | Google AuthorizationClientで許可を取得し、Driveのアプリ専用領域で作品ハッシュと復帰位置を同期。更新日時で競合を解決 |

## 検証

以下は2026-09-12時点の検証記録: Instrumentation **67項目成功**、自然順ソートテスト **14項目成功**。Lint **0 errors / 28 warnings**。リファクタリングの内容は [DEVELOPMENT.md](DEVELOPMENT.md) に記載。

- 元のインストール済みアプリを残すため、`jp.yaman.comicexplorer.validation` を別アプリとしてエミュレーターへインストール。
- テスト用CBZ／PDFを生成して、ページ数・デコード・縦スクロール・位置更新・画像処理を確認。
- 復帰位置としおりの個別削除、名前変更時の読書情報維持、設定初期化、キャッシュ削除、同期JSONのマージを検証。
- FTPはループバックの検証サーバーでパッシブ一覧取得・バイナリ取得・接続終了を確認。実サーバー全般との互換性を保証する検証ではない。
- 日本語／英語、明色／暗色の設定画面をエミュレーターで撮影し、文字と配置を目視確認。
- Android 17／API 37のPixel 7aエミュレーターで、履歴グリッド、横画面PDF、横画面の切り抜きダイアログも撮影・目視確認。切り抜きの下側操作はスクロールで表示。
- Lintはエラーなし。部分翻訳の英語フォールバックはリソース側で意図を明示して検査対象から除外。依存ライブラリ内の未使用TrustManager、APIの推奨変更、文字連結などの警告は残る。
- 実行可能なテスト: `app/src/androidTest/java/jp/yaman/comicexplorer/ParityInstrumentation.java`、`tests/ComicFileTest.java`。
- 画像: `build/parity-screenshots/`。ビルド生成物のためGit管理外。

## 完全一致ではない点・確認待ち

- 元APKは起動時にGoogle Play画面へ遷移するため、実際の閲覧UIとの画素単位の比較はできていない。元APKのリソースと上記の対象処理の静的解析を根拠とする対応であり、アイコン形状・余白・アニメーション・全初期値の一致は保証しない。
- Google DriveのOAuthクライアント登録と認証、2台間の実同期は未検証。SMB／FTPSの実サーバー接続と、ネットワーク上の変更操作も未検証。
- 補間方式名などの技術用語は共通表記。翻訳はネイティブ話者による校閲を行っていない。
- サイド一覧・色パレット・サムネイル帯の画素単位の一致は未検証。章はアーカイブ内フォルダまたはPDFの目次から取得する。
- 切り抜きは作品別保存。元アプリの正確な保存単位は未確定。端末間同期は復帰位置のみ。
- アルバムは元画像への参照を保存する方式。元アプリとのアルバムデータ互換はない。
- 1回のフォルダ転送は20,000項目・深さ64まで。一時領域が必要。書き込み・名前変更・削除・パス確認に非対応の保存先では操作を拒否する。同時に別アプリから更新されるデータへの原子的な移動は保証しない。
- ソリッド圧縮のランダム移動では先行データの展開が必要で、大規模な作品は遅くなる場合がある。RAR5は自作の無圧縮フィクスチャ、暗号化は7z/PDFを中心に検証しており、すべての圧縮方式・暗号方式の組み合わせは未検証。AVIFは端末デコーダー依存。GIFは約30fpsを上限とする。
- 広告・購入導線は追加していない。元アプリとのデータ互換は上記の限定取り込みのみで、アカウント共用はない。

「全項目を検討して対応を広げた」状態であり、上記の残差を含む完全互換版ではない。
