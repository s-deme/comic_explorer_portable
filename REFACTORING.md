# 2026-09-20 構造リファクタリング

## 洗い出しと実施範囲

本棚・読書画面、画像／アーカイブ読込、キャッシュ、ファイル転送、ネットワーク、保存・同期・インポートの境界と呼び出しを確認した。軽微な整形・命名変更を除き、今回具体化した次の6案をすべて実施した。

| 案 | 問題と実施内容 |
|---|---|
| 本棚サムネイルの責務分離 | `MainActivity` のアダプターがワーカー・メモリーキャッシュ・ディスクキャッシュ・画像デコードまで管理していた。`LibraryThumbnails` に集約し、通常画像と独自表紙の表示更新を共通化。再バインド・キャッシュ無効化・終了後の古い処理結果を破棄する。 |
| ページ一覧の責務分離 | `showPageBrowser` 内の匿名アダプターと可変配列の終了フラグを、`PageBrowserAdapter` の所有するページ／章一覧と終了状態へ移した。ダイアログとドロワーは同じアダプターを使い、元の本が変わった結果も破棄する。 |
| 静止画像デコードの統合 | URI画像とZIP画像で重複したサイズ調査・妥当性確認・縮小デコードを `decodeImage` へ統合。入力ストリームの開き方のみ分岐し、ZIPの展開サイズ制限と各形式のエラー表示を保持。 |
| 安全なコピー処理の共通化 | 本のキャッシュ、ネットワークのダウンロード／SMBアップロード、ファイル転送、GIF読込のコピーを `StreamCopy` に集約。中断・空き容量チェックを共通化し、ハッシュが必要な転送だけ標準の `DigestInputStream` を組み合わせる。ストリームのclose、ファイルのsync、成功後の確定は呼び出し側が担当する。 |
| 転送先照合の効率化 | フォルダ内の各項目について転送先を全件検索する処理を、名前の `HashMap` に統合。階層ごとの照合は平均 O(n²) から O(n) へ。重複名・種別不一致・照合漏れを拒否し、移動前の全内容ハッシュ検証を維持する。 |
| メニュー分岐の統合 | 本棚のアプリメニューとファイル操作メニューを既存の `Ui.Actions` へ移行。表示文字列の比較、並行した番号配列、条件付き項目の位置に依存した分岐を削除。表示順・文言・実行処理は保持する。 |

## 対象外にした案

- import整理、全面的な改行・命名統一、単純ラッパーの削除だけの変更は軽微なため見送った。
- `AppState` の機械的なファイル分割、MVVM・DI導入、画面全体の置き換えは、今回の重複や所有権の問題を解決するために必要なく、採用しなかった。
- 同期レスポンスと設定インポートは異なる入力サイズ上限を持つため、専用の制限付き読込を保持した。

保存キー・保存形式・依存ライブラリは変更していない。通常版アプリと配布APKは更新しない。

## 検証

検証専用applicationId `jp.yaman.comicexplorer.validation` を使用する。追加した8チェックは、複数バッファのコピーとSHA-256、中断、空き容量不足、URI／ZIPの画素一致、ページ一覧の置き換えと章境界、独自表紙の読込、無効化済みサムネイルの破棄、条件付きメニュー操作の対象保持。転送の置換・移動・失敗時の原本保持は既存チェックで確認する。

検証APK・テストAPKのビルド成功。Lintは **0 errors / 39 warnings**。接続済みAndroidエミュレーターで `suite all` を実行し、**PASS [all]: 123 checks**（既存115＋追加8）を確認した。`git diff --check` も成功。

記録は `build/refactor-structure-build.txt`、`build/refactor-structure-test.txt`、`app/build/reports/lint-results-debug.html`。生成時のデバッグ鍵とインストール済み検証アプリの鍵が異なったため、証明書のSHA-256が一致する既存鍵で検証APKだけを再署名して更新した。

実サービスへのSMB／FTPS接続・Google Drive同期、実機での操作、画面の目視確認は今回の検証に含めない。ネットワークの自動検証は既存の模擬FTPサーバーとパス境界テストを使用した。
