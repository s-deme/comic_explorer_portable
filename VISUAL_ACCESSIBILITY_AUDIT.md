# 可読性・人間工学監査（v1.1.2）

対象はライブラリ、設定、読書画面、エラー、選択ダイアログです。目標は「読めること」と「迷わず操作できること」を、視力・屋外・暗所・片手操作を含めて担保することです。

## 適用した基準

- 通常文字は濃い前景色と明るい背景の組み合わせに統一し、本文・補助文とも 14sp 以上にしました。重要な本文は 16sp 以上です。
- 主操作は濃い紫地に白文字、副操作は明るいサーフェスと境界線を持たせ、選択中のタブは文章とアクセシビリティ状態でも伝えます。
- 読書画面は暗色の不透明サーフェス上に白文字のボタンを置きます。以前のように、暗い背景へ白文字だけを置いて標準ボタン背景に依存しません。
- 無効操作は低い不透明度で消すのではなく、専用の薄灰背景と濃い灰文字で状態を読めるようにします。
- すべての操作は 48dp 以上、チェック・ラジオ項目は 52dp 以上にします。文字とアイコンの見た目だけに操作を委ねません。
- 機能が分かりにくい記号（更新、設定、戻る、しおり、メニュー、上へ）は日本語ラベルへ置換します。スクリーンリーダー向け説明も残します。
- 検索・数値入力・スライダー・チェック状態には、明示的な枠線／つまみ／色を設定し、OSテーマによる薄い表示に依存しません。

代表的な組み合わせをWCAGの相対輝度式で再確認し、通常文字のAA基準（4.5:1）を上回っています。主本文 16.03:1、補助本文 8.08:1、薄い補助本文 5.42:1、紫地の白文字 7.41:1、読書画面の本文 15.49:1、読書画面の主操作 9.08:1、明色／暗色の無効ボタンは 4.62:1／6.01:1 です。

## 修正した問題

| 監査対象 | 問題 | 修正 |
| --- | --- | --- |
| ライブラリ | 補助文字が12〜13spかつ淡色、記号ボタンのみ | 14sp以上の濃色へ。更新・設定・上へ・保存をラベル化 |
| 検索 | ヒントと入力枠がOSテーマ任せ | 濃い入力文字、読めるヒント、白地と明確な枠線 |
| 一覧 | 種類・復帰位置が小さく薄い、スター操作が曖昧 | 14spへ、濃色へ、「保存／保存済」に変更 |
| 設定 | デフォルトのチェック・ラジオ・破壊的操作が曖昧 | 高コントラストの選択色、52dp行、危険操作を赤い「消去」ボタンに |
| 読書画面 | 白文字の標準ボタンが明るい背景に乗り得る。記号のみの操作 | 不透明な暗色または青のボタン、戻る・しおり・メニューの明示ラベル |
| 進行操作 | スライダーの線・つまみが背景に埋もれ得る | 明示的な高コントラストの進捗・トラック・つまみ |
| ダイアログ | 操作ボタンの濃さが端末テーマに左右される | 表示時に高コントラストの主／副ボタンを適用 |

## v1.1.2 の刷新

- 検索、表示タブ、フォルダ操作、現在位置、作品一覧の順に情報階層を整理しました。
- 未選択、読み込み中、空フォルダ、検索0件、お気に入り0件、履歴0件、アクセスエラーを個別の空状態として表示します。
- 作品行を表紙・形式・容量・復帰位置・保存状態が走査しやすいカードへ変更しました。
- 設定を「読書体験」と「端末内データ」のカードへ分け、破壊的操作は赤いトーン面と確認ダイアログで区別しました。
- 高さが限られる横画面では検索と表示タブ、操作と現在位置を横並びにし、作品一覧の表示領域を確保します。
- キーボードフォーカスには2dpの明示枠、チェック項目には選択／未選択／無効の個別色を設定しました。

## 継続して守るルール

- 新しい画面で色だけに意味を持たせない。状態は文章でも示す。
- 12sp以下の本文・補助文は追加しない。長い説明は折り返せるようにする。
- 背景画像や半透明レイヤー上に文字を直接置かない。必ず不透明または十分に濃いサーフェスを敷く。
- 記号を使う場合も、視覚上の日本語ラベルまたは同じ行の説明を必ず併記する。
